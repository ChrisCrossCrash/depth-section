{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: storybook",
      "request": "launch",
      "command": "yarn run storybook",
      "cwd": "${workspaceFolder}"
    }
  ]
}